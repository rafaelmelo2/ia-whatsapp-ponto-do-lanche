// Entrypoint do serviço Worker (consome a fila e processa pedidos via LLM).
// Stub do Épico 0 — implementação real no Épico 5.
import { logger } from "@sirvase/core";

logger.info("worker stub — sem implementação ainda (Épico 5)");
